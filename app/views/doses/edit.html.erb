
<%= simple_form_for([@cocktail, @dose]) do |f| %>
<%= f.input :description, placeholder: "Great recipe!" %>
<%= f.select :ingredient_id, options_from_collection_for_select(Ingredient.all, :id, :name)%>
<%= f.submit "Add dose", class: "btn btn-primary" %>
<% end %>
